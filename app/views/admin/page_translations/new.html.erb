<%= turbo_frame_tag "modal" do %>
  <div class="modal-header">
    <h1 class="modal-title">New</h1>
  </div>
  <%= render 'form', service: @service, page_template: @page_template, page_translation: @page_translation %>
<% end %>
