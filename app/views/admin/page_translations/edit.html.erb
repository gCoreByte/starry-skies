<%= turbo_frame_tag "modal" do %>
  <div class="modal-header">
    <h1 class="modal-title"><%= t('edit') %></h1>
  </div>
  <%= render 'form', service: @service, page_translation: @page_translation %>
<% end %>
