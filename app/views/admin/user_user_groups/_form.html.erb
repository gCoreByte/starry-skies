<%= form_with(model: [:admin, @user_account, @user_user_group]) do |form| %>
  <div class='modal-body'>
    <% if @user_user_group.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(@user_user_group.errors.count, "error") %> prohibited this product category from being saved:</h2>
        <ul>
          <% @user_user_group.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <div class='mb-3'>
      <%= form.label :user_group, { class: 'form-label' } %>
      <%= form.collection_select :user_group_id, @store.user_groups, :id, :key, {}, { class: 'form-select'} %>
    </div>
  </div>

  <div class='modal-footer'>
    <%= form.submit 'Save', class: 'btn btn-primary' %>
  </div>
<% end %>
