<%= turbo_frame_tag "modal" do %>
  <div class="modal-header">
    <h1 class="modal-title"><%= t('edit') %></h1>
  </div>
  <%= render 'form', service: @service, product_version: @product_version, product_price: @product_price %>
<% end %>
