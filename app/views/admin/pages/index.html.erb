<p style="color: green"><%= notice %></p>

<%= page_title('Pages') do %>
  <%= create_button(:admin, :store, :page) %>
<% end %>

<div>
  <div class="row">
    <div class="col-lg-12">
      <div class="card mb-3 border">
        <div class="table-responsive">
          <table class="table table-bordered table-striped">
            <thead>
              <th>Key</th>
              <th>Status</th>
              <th>URL</th>
              <th>Actions</th>
            <tbody id=pages>
              <%= render @pages %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
