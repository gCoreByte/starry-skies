<%= page_title(t('titles.pages')) do %>
  <%= create_button(:admin, :store, :page) %>
<% end %>

<div>
  <div class="row">
    <div class="col-lg-12">
      <div class="card mb-3 border">
        <div class="table-responsive">
          <table class="table table-bordered table-striped">
            <thead>
              <th><%= t('attributes.key') %></th>
              <th><%= t('attributes.status') %></th>
              <th><%= t('attributes.url') %></th>
              <th><%= t('attributes.actions') %></th>
            <tbody id=pages>
              <%= render @pages %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
