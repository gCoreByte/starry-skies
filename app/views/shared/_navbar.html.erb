<nav class="navbar navbar-expand-lg navbar-dark navbar-background">
  <div class="container">
    <%= link_to image_tag('starryskies.png', size: 50), root_path, class: 'navbar-brand me-4' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto gap-3">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            StarrySkies
          </a>
          <ul class="dropdown-menu">
            <li class="nav-item">
              <%= link_to 'Home', root_path, class: 'nav-link text-dark' %>
            </li>
          </ul>
        </li>
        <% if @current_user.present? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Account Management
            </a>
            <ul class="dropdown-menu">
              <li class="nav-item">
                <%= link_to 'Dashboard', admin_dashboard_index_path, class: 'nav-link text-dark' %>
              </li>
              <li class="nav-item">
                <%= link_to 'Variables', variables_admin_documentations_path, class: 'nav-link text-dark' %>
              </li>
            </ul>
          </li>
          <% if @current_store.present? %>
            <li class="nav-item">
              <%= link_to 'Store', admin_store_path(@current_store), class: 'nav-link' %>
            </li>
          <% end %>
        <% end %>
      </ul>
      <ul class="navbar-nav gap-3">
        <div class="dropdown">
          <button class="nav-link dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            Locale
          </button>
          <div class="dropdown-menu" aria-labelledby="localeDropdown">
            <a class="dropdown-item" href="#" onclick="changeLocale('en')">🇬🇧 English</a>
            <a class="dropdown-item" href="#" onclick="changeLocale('et')">🇪🇪 Eesti</a>
            <a class="dropdown-item" href="#" onclick="changeLocale('ru')">🇷🇺 Русский</a>
          </div>
        </div>
      </ul>
      <% unless @current_user.present? %>
        <ul class="navbar-nav gap-3">
          <li class="nav-item">
            <%= link_to 'Sign Up', sign_up_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign In', sign_in_path, class: 'nav-link' %>
          </li>
        </ul>
      <% end %>
      <% if @current_user.present? %>
        <ul class="navbar-nav gap-3">
          <li class="nav-item">
            <%= button_to 'Sign Out', session_path(Current.session), method: :delete, class: 'nav-link' %>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
